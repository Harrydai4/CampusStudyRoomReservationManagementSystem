# 最终验收对照（问题记录 + 原型 + 设计文档）

> **版本**：V1.1 最终版 · **核对日期**：2026-05-26  
> **真源**：`docs/07-原型资源/原型设计.html` · `docs/05-需求与设计/概要设计与详细设计.md` §8–§9  
> **问题台账**：`问题记录.md`（10 条均已 ✅）

---

## 1. 结论（答辩可用）

| 维度 | 结论 |
|------|------|
| 问题记录 1～10 | 均已对照代码验收通过 |
| 原型 UI（M00–M21） | 见 `UI对齐总工作文档.md`，阶段 P0–P4 均为 ✅ |
| 后端 API §7 | `AppController` 映射完整 |
| 构建 | `mvnw test`、`build-frontend.ps1` 通过 |
| 已知可说明差异 | Element Plus 控件外观、修改密码 V1.2、seatMap 字符串编辑方式 |

---

## 2. 问题记录 × 实现位置速查

| # | 验收要点 | 关键文件 |
|---|----------|----------|
| 1 | 真实 QR + 学生信息 | `qr.js`, `App.vue` loadQr, `AppService.qrCode` |
| 2 | 摄像头扫码 | `App.vue` startCameraScan |
| 5 | 先扫码提示 | `AppService.scanCheckin` |
| 6 | 先开摄像头 | `App.vue` startCameraScan 顺序 |
| 7–8 | 学生 UI + 手机紧凑 | `App.vue`, `prototype-ui.css`, `styles.css` |
| 9 | 10 分钟、弹窗、分布图 | `App.vue` 预约页, `AppService.slots` |
| 10 | 统计标题/柱图 | `studyChartTitle`, `bar-track`, `myStudyDuration` |

---

## 3. 原型页面对照（抽样）

| 原型 | 实现 | 状态 |
|------|------|:---:|
| `#page-login` | `!token` 学生登录 | ✅ |
| `#page-reservation` | 日期 pill、快捷时段、分区座位、确认弹窗 | ✅ |
| `#page-checkin` | 计时、暂离、QR、签退摘要 | ✅ |
| `#qrModal` | `qrModalOpen` | ✅ |
| `#page-admin-login` | `admin-login-card` | ✅ |
| 管理端仪表盘四 KPI | `admin-dashboard-grid` | ✅ |

完整台账 → `UI对齐总工作文档.md` §4。

---

## 4. 设计文档 §9 核心流程（必演示）

1. 学生登录 `202301010101` / `123456`  
2. 首页 → 预约 → 选座 → 确认 → 签到页  
3. 出示二维码 → 管理员扫码或粘贴 token → 使用中  
4. 暂离 → 返回 → 签退  
5. 管理端：用户审核、公告、统计  

步骤清单 → `docs/01-使用指南/02-答辩验收清单.md`

---

## 5. 提交 GitHub 前最后 5 项

| # | 检查 |
|---|------|
| 1 | 无 `application-local.properties` 在 git 中 |
| 2 | `static/index.html` 已 build |
| 3 | PR 指向 `v1.1-harrydai4`（组员不直接改 master） |
| 4 | 发给组长 PR 链接 |
| 5 | 本地 8080 能登录 |

流程 → `docs/01-使用指南/06-GitHub傻瓜式提交全流程.md`

---

## 6. 允许差异（口述）

- Element Plus 与原型原生控件外观略异，交互一致  
- 修改密码入口 toast「待接入」  
- 座位编辑为属性网格，非 HTML seatMap 字符串  

---

**最终版交付物**：可运行 `CSRRMupdate` + 完整 `docs/` + 已开 PR（组员）或已合并 master（组长合并后）。
