# =============================================================================
# Shared team environment (copy to application-shared.properties, do NOT commit)
# =============================================================================
#   Copy-Item application-shared.properties.example application-shared.properties
#   Then: .\scripts\start-shared-server.ps1
# Doc: docs/01-使用指南/07-多人共用一套系统与数据库.md
# =============================================================================

# MySQL on same machine as Spring Boot
spring.datasource.url=jdbc:mysql://127.0.0.1:3306/study_room_reservation?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=study
spring.datasource.password=csrrm_shared_123

# Upload dir (absolute path on app server)
app.upload.dir=D:/SchoolWorkPlace/Database/CSRRMupdate/uploads

# Optional
# spring.datasource.hikari.maximum-pool-size=20
# app.demo.sync-accounts-on-startup=true
